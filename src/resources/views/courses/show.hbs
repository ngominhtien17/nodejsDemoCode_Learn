<div class="mt-4">
    <div class="row">
        <div class="col-sm-6 col-lg-4">
            <div class="card" style="width: 18rem; margin-top: 16px;">
                    <img src="{{courses.img}}" class="card-img-top" alt="Card iamge cap">
            <div class="card-body">
                    <h5 class="card-title">{{courses.name}}</h5>
                <p class="card-text">{{courses.description}}</p>
                <a href="#" class="btn btn-primary">Buy Course</a>
            </div>
        </div>
    </div>
</div>